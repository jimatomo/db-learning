services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      LESSON: c
      PORT: "8080"
      DATABASE_PATH: /data/app-c.db
      RUN_SEED: "1"
    volumes:
      - db_data:/data

volumes:
  db_data:
