{
  "name": "@db-learning/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun --minify",
    "start": "bun src/index.ts",
    "seed": "bun run scripts/seed.ts"
  },
  "dependencies": {
    "@db-learning/db": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "bun-types": "^1.2.2"
  }
}
